// WebSocket event type definitions placeholder
export interface ClientToServerEvents {
  // define client to server events
}

export interface ServerToClientEvents {
  // define server to client events
}
